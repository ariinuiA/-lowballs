<h1>Item Details</h1>

<p>
  <strong>Title:</strong> <%= @item.title %>
</p>
<p>
  <strong>Description:</strong> <%= @item.description %>
</p>
<p>
  <strong>Price:</strong> <%= number_to_currency(@item.price.to_f / 100, unit: "$") %>
</p>
<p>
  <strong>Owner:</strong> <%= @item.user.first_name %> <%= @item.user.last_name %>
</p>

<%= link_to 'Back to Items', items_path %>
<% unless @item.user == current_user %>
| <%= link_to 'Book item', new_item_booking_path(@item) %>
<% end %>
