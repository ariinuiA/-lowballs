<h1><%= @booking.item.title %></h1>
<h2><%= @booking.status %></h2>
<%= simple_form_for [@item, @booking] do |f| %>
  <%= f.input :status, :collection => %w[Approved Refused] %>

  <%= f.button :submit, "Submit Booking" %>
<% end %>
